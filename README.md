# seox-auditor
on-page seo auditor, extracts the top 20 metrics through the sites' REST apis, that Google rates your site optimization on and returns a wellness score as well as things about your site pages that you should fix.
